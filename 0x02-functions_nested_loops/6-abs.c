#include "main.h"

/**
 *  *  _abs - Entry point
 *   *  Description:'the program's description'
 *    *  @n : the character to check
 *     *  Return: Always 0 (Success)
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
